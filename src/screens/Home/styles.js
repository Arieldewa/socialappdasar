import { ImageBackground, View, Dimensions, ScrollView, TouchableOpacity, Picker, Image } from "react-native";
const deviceHeight = Dimensions.get('window').height;
const deviceWidth = Dimensions.get('window').width;
export default {
  containerImage: {
    flex: 1,
    width: null,
    height: null
  },  
};
